# Windows 图形管线与帧时间

## 基本流程

游戏画面从 CPU 提交任务，到 GPU 渲染，再到显示输出，需要经过多个阶段：

CPU 游戏逻辑 → DirectX → GPU 队列 → 显示合成 → 显示器刷新

## 帧时间的重要性

平均 FPS 不能完全反映体验。稳定的帧时间通常比单纯提高平均帧率更重要。

需要关注：

- 帧时间波动
- 1% Low FPS
- GPU 使用率变化
- CPU 瓶颈情况

## 与优化工具的关系

优化项目应该围绕减少不必要的调度开销和保持稳定性，而不是简单关闭所有功能。
